package main

import (
	"log"

	"github.com/ecommerce/configure"
)
func main(){
config,configErr:= configure.LoadConfig()
if configErr!=nil{
	log.Fatal("cannot load config",configErr)
}
server,dberr:=

}




















































































































